SELECT * FROM Sintomas;
Select des_sint From Sintomas Where id_sint IN (Select id_sint From Causas Where id_patol = 2);
Select nom_patol, trat_patol From Patologias Where id_patol IN (Select id_patol From Causas where id_sint =5);

-------------------------------------------
-- 1. Buscar patologias por nombre.
-- 2. Buscar sintomas por nombre.
-- 3. Listar patologias.
-- 4. Buscar lista patologia por sintomas.
-------------------------------------------

1. --- Buscar patologias por nombre ---
    SELECT * FROM Patologias;
    SELECT nom_pato1 FROM Patologias WHERE id_pato1, :nom_pato1;
    SELECT * FROM Patologias WHERE nom_pato1 LIKE :nom_pato1 %; 

    SELECT * FROM Patologias;
    SELECT nom_pato1 From Patologias Where id_pato1 IN (Select id_pato1 From Patlogias Where :id_patol);
    SELECT nom_patol, trat_patol From Patologias Where id_patol IN (Select id_patol From Patologias where :id_pato1);
  
2. --- Buscar sintomas por nombre ---
    SELECT * FROM Sintomas;
    SELECT nom_sint FROM Sintomas WHERE id_sint, :nom_sint;
    SELECT * FROM Sintomas WHERE nom_sint LIKE :nom_sint %; 

    SELECT * FROM Sintomas;
    SELECT nom_sint From Sintomas Where id_sint IN (Select id_sint From Sintomas Where :id_sint);
    SELECT nom_sint, trat_sint From Sintomas Where id_sint IN (Select id_sint From Sintomas where :id_sint);
  
3. --- Listar patologias ---
    SELECT * FROM Patlogias
    SELECT ALL FROM Patlogias;
    SELECT nom_pato1 FROM Patologias WHERE nom_pato1;
    
4. --- Buscar lista patologia por sintomas ---


   
